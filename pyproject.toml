[build-system]
requires = ["hatchling<1.32"]  # 1.32 defaults core metadata to 2.5, which current PyPI upload tooling rejects
build-backend = "hatchling.build"

[project]
name = "solace-agent-mesh"
dynamic = ["version"]
description = "Solace Agent Mesh is an open-source framework for building event-driven, multi-agent AI systems where specialized agents collaborate on complex tasks."
readme = "README.md"
requires-python = ">=3.10.16,<3.14"
license = { file = "LICENSE" }
authors = [
  { name="SolaceLabs", email="solacelabs@solace.com" },
]
classifiers = [
    "Development Status :: 7 - Inactive",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: OS Independent",
    "Topic :: Software Development :: Libraries :: Application Frameworks",
]
dependencies = [
    "google-adk==1.18.0",
    "mcp==1.28.1",  # [CVE-2026-59950, CVE-2026-52870, CVE-2026-52869] Security fix: pins transitive dep (from google-adk) to 1.28.1
    "a2a-sdk[http-server]==0.3.7",
    "pydantic==2.12.5",
    "click==8.3.3",  # [CVE-2026-7246] Security fix
    "python-dotenv==1.2.2",
    "google-genai==1.49.0",
    "httpx==0.28.1",
    "idna==3.15",  # [CVE-2026-45409] Security fix (transitive from httpx, requests)
    "jwcrypto==1.5.7",  # [CVE-2026-39373] Security fix
    "python-jwt==4.1.0",
    "pyjwt>=2.13.0",  # [CVE-2026-48526] Security fix: rejects JWK JSON passed as an HMAC secret
    "asteval==1.0.6",
    "pystache==0.6.8",
    "python-liquid==2.2.1",  # [CVE-2026-55865] Security fix: DoS via malformed {% case %} tag (parse-time infinite loop)
    "pandas==2.3.2",
    "numpy==2.2.6",
    "plotly==6.3.0",
    "kaleido==0.2.1",
    "PyYAML==6.0.2",
    "python-multipart==0.0.30",  # [CVE-2026-53539] Security fix: DoS via malformed multipart input
    "wheel==0.46.2",
    "jaraco.context==6.1.0",
    "protobuf==6.33.6",
    "setuptools==80.10.2",
    "markitdown[all]==0.1.4",
    "jmespath==1.0.1",
    "mermaid_cli==0.1.2",
    "playwright==1.58.0",
    "litellm==1.84.0",  # bumped from 1.83.14 to relax its aiohttp==3.13.4 pin, allowing aiohttp >=3.14.0 (locked to 3.14.1) [CVE-2026-34993, CVE-2026-47265]
    "markdownify==1.2.0",
    "beautifulsoup4==4.13.5",
    "soupsieve==2.8.4",  # [CVE-2026-49476, CVE-2026-49477] Security fix (transitive from beautifulsoup4): DoS via unbounded memory in selector-list compile & regex catastrophic backtracking
    "jsonpath-ng==1.7.0",
    "pydub==0.25.1",
    "toml==0.10.2",
    "GitPython==3.1.59",
    "Flask==3.1.3",
    "flask-cors==6.0.1",
    "werkzeug==3.1.6",
    "fastapi==0.120.1",
    "starlette==0.49.1",  # [CVE-2026-48818, CVE-2026-54283] only fixed in starlette 1.x, which google-adk caps out (<1.0.0); cannot upgrade until google-adk supports it
    "uvicorn[standard]==0.37.0",
    "sse-starlette==3.0.2",
    "itsdangerous==2.2.0",
    "solace_ai_connector==3.3.12",
    "holidays==0.81.0",
    "rouge==1.0.1",
    "SQLAlchemy==2.0.40",
    "alembic==1.16.5",
    "mako>=1.3.12",  # [CVE-2026-44307, CVE-2026-41205] Security fix: path traversal vulnerabilities (transitive from alembic)
    "openai==2.24.0",
    "rich==13.9.4",
    "boto3==1.40.37",
    "psycopg2-binary==2.9.10",
    "azure-cognitiveservices-speech==1.41.1",
    "cryptography==48.0.1",  # [CVE-2026-39892, GHSA-537c-gmf6-5ccf] Security fix: addresses vulnerabilities in older versions (transitive dependency from multiple packages)
    "pillow==12.3.0",  # [CVE-2026-54058, CVE-2026-54059, CVE-2026-54060, CVE-2026-55379, CVE-2026-55380, CVE-2026-55798, CVE-2026-59198, CVE-2026-59203, CVE-2026-59204] Security fix: addresses vulnerabilities in older versions (transitive dependency from python-pptx)
    "bm25s==0.2.14",
    "pypdf==6.15.0",  # Security fix: multiple vulnerabilities resolved in 6.15.0
    "lxml==6.1.0",
    "python-pptx==1.0.2",
    "python-docx==1.2.0",
    "google-cloud-storage==3.9.0",
    "azure-identity>=1.17.0",
    "azure-storage-blob==12.28.0",
    "authlib>=1.6.12",  # [CVE-2026-27962, CVE-2026-41425, CVE-2026-44681] Security fixes
    "pyasn1>=0.6.4",  # [CVE-2026-30922, CVE-2026-59884, CVE-2026-59885, CVE-2026-59886] Security fixes
    "cachetools==7.0.1",
    "openfeature-sdk==0.8.4",
    "filelock==3.20.3",
    "uuid-utils>=0.11.1",
    "nanoid==2.0.0",
    "APScheduler==3.10.4",
    "croniter==2.0.5",
]

[project.optional-dependencies]
vertex = ["google-cloud-aiplatform==1.133.0"]
employee_tools = ["holidays==0.81.0"]
test = [
    "pytest-asyncio",
    "pytest>=8.0.0",
    "pytest-mock>=3.0.0",
    "pytest-cov>=4.0.0",
    "pytest-xdist>=3.5.0",
    "pytest-httpx>=0.35.0",
    "fastmcp>=3.2.0",  # Upgrade to fix SSRF/path traversal (GHSA-vv7q-7jx5-f767)
    "httpx>=0.25",
    "respx",
    "ruff",
    "testcontainers[postgres,mysql]>=4.0.0",
    "aiosqlite",
    "psycopg2-binary",
    "asyncpg",
    "pymysql>=1.1.0",  # Required for MySQL migration tests
]

[tool.hatch.envs.hatch-test]
parallel = false
installer = "uv"
dependencies = [
    "solace-agent-mesh[vertex,employee_tools]",
    "pytest-asyncio",
    "pytest>=8.0.0",
    "pytest-mock>=3.0.0",
    "pytest-cov>=4.0.0",
    "pytest-xdist>=3.5.0",
    "pytest-httpx>=0.35.0",
    "fastmcp>=3.2.0",  # Upgrade to fix SSRF/path traversal (GHSA-vv7q-7jx5-f767)
    "httpx>=0.25",
    "respx",
    "ruff",
    "testcontainers[postgres,mysql]>=4.0.0",
    "aiosqlite",
    "psycopg2-binary",
    "asyncpg",
    "bm25s==0.2.14",
    "pymysql>=1.1.0",  # Required for MySQL testcontainers
    "pypdf==6.15.0",
    "python-pptx==1.0.2",
    "python-docx==1.2.0",
    "sam-test-infrastructure @ {root:uri}/tests/sam-test-infrastructure"
]
post-install-commands = [
  "playwright install"
]

[[tool.hatch.envs.hatch-test.matrix]]
python = ["3.10", "3.13"]

[tool.hatch.envs.hatch-test.scripts]
run = "pytest {args}"
run-cov = "coverage run -m pytest --cov=src {args}"
cov-combine = "coverage combine"
cov-report = "coverage report"


[project.urls]
Homepage = "https://github.com/SolaceLabs/solace-agent-mesh"
Repository = "https://github.com/SolaceLabs/solace-agent-mesh"

[project.scripts]
solace-agent-mesh = "solace_agent_mesh.cli.main:cli"
sam = "solace_agent_mesh.cli.main:cli"

[tool.ruff]
line-length = 88
select = ["E", "W", "F", "I", "UP", "B", "C4", "SIM"]
ignore = ["E501", "B008"]
# The CLI, evaluation and config-portal backends moved under src/ so the
# console entry points resolve in editable installs (#1645). They lived
# outside the linted roots (./src ./tests) before the move and carry that
# period's style debt; excluding them keeps the move a move, not a rewrite.
extend-exclude = [
  "src/solace_agent_mesh/cli",
  "src/solace_agent_mesh/evaluation",
  "src/solace_agent_mesh/config_portal",
]

[tool.ruff.format]
quote-style = "double"

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
addopts = "--tb=short --strict-markers --disable-warnings -p pytest_asyncio --ignore=tests/system --ignore=tests/stress"
testpaths = ["tests"]
pythonpath = [".", "src", "tests/sam-test-infrastructure/src"]
python_files = ["test_*.py", "*_test.py", "tests.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
markers = [
    "all: marks all tests",
    "asyncio: marks tests as async (deselect with '-m \"not asyncio\"')",
    "default: marks tests that are basic or foundational",
    "stress: marks tests as stress tests (long-running, resource-intensive)",
    "long_soak: marks tests as very long-running soak tests for memory leak detection",
    "slow: marks tests as slow-running (used by integration migration suites)",

    # Core Components
    # Markers for the main components of the Solace Agent Mesh.
    "agent: marks tests related to the agent component",
    "common: marks tests related to the common component",
    "core_a2a: marks tests related to the core_a2a component",
    "gateway: marks tests related to the gateway component",

    # Common Components
    # Markers for sub-components within the 'common' directory.
    "middleware: marks tests related to the middleware component",
    "server: marks tests related to the server component",
    "services: marks tests related to the services",

    # Features
    # Markers for specific features.
    "callbacks: marks tests related to agent callbacks",
    "datapart: marks tests related to A2A DataPart objects",
    "delegation: marks tests related to agent delegation",
    "embeds: marks tests related to the embeds",
    "mcp: marks tests related to mcp",
    "notification: marks tests related to notifications",
    "task_cancellation: marks tests related to task cancellation",
    "proxy: marks tests related to the A2A proxy functionality",
    "streaming: marks tests related to streaming messages",
    "artifacts: marks tests related to artifact handling and services",
    "workflows: marks tests related to workflow execution",
    "map: marks tests for map (parallel iteration) workflows",
    "switch: marks tests for switch (multi-way) workflow branching",
    "loop: marks tests for loop workflow iteration",
    "api: marks tests related to the API",
    "tasks: marks tests related to task management",
    "builtin_artifact_embeds: marks tests related to the builtin_artifact_embeds",
    "projects: marks tests related to project management",
    "default_agent: marks tests related to default agent functionality",
    "crud: marks tests related to CRUD operations",
    "templates: marks tests related to template operations",
    "pagination: marks tests related to pagination functionality",
    "security: marks tests related to security and authorization",
    "error: marks tests related to error handling scenarios",
    "validation: marks tests related to input validation",
    "prompts: marks tests related to the prompts library feature",
    "versioning: marks tests related to version management",
    "search: marks tests related to search and filtering functionality",

    # Agent Tools
    # Markers for the built-in agent tools.
    "tools: marks tests related to the agent tools",
    "audio_tools: marks tests related to the audio_tools",
    "peer_tool: marks tests for calling peer agents",
    "parallel_tool: marks tests related to the parallel_tool",
    "builtin_artifact_tools: marks tests related to the builtin_artifact_tools",
    "builtin_data_analysis_tools: marks tests related to the builtin_data_analysis_tools",
    "general_agent_tools: marks tests related to the general_agent_tools",
    "image_tools: marks tests related to the image_tools",
    "peer_agent_tool: marks tests related to the peer_agent_tool",
    "web_tools: marks tests related to the web_tools",
    "web_search: marks tests related to web search tools (Tavily, Google, Exa, Brave)",
    "deep_research: marks tests related to deep research tool",
    "dynamic_tools: marks tests related to the dynamic_tools",
    "artifact_preloading: marks tests related to ArtifactContent type hint pre-loading",
    "auth: marks tests associated with authentication and authorization",
    "oauth: OAuth specific tests",
    "error_handling: Tests related to error handling and recovery",
    "cancellation: Tests related to task cancellation",
    "headers: marks tests related to HTTP header handling",
    "agent_card: marks tests related to agent card fetching and discovery",
    "url: marks tests related to URL handling and routing",
    "task: marks tests related to task invocation",
]

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/solace_agent_mesh"]

[tool.hatch.build.targets.wheel.force-include]
# Only non-Python build outputs still need grafting: the Python packages
# (cli, evaluation, config_portal.backend, templates) live under
# src/solace_agent_mesh and ship with the package in every install mode,
# editable included (#1645).
"config_portal/frontend/static" = "solace_agent_mesh/config_portal/frontend/static"
"client/webui/frontend/static" = "solace_agent_mesh/client/webui/frontend/static"
"docs/build" = "solace_agent_mesh/assets/docs"

[tool.hatch.build.targets.sdist.force-include]
"config_portal/frontend/static" = "/assets/config_portal"
"client/webui/frontend/static" = "/assets/client/webui"
"docs/build" = "/assets/docs"

[tool.hatch.build.targets.sdist.hooks.custom]
path = ".github/helper_scripts/build_frontend.py"

[tool.hatch.build.targets.wheel.hooks.custom]
path = ".github/helper_scripts/build_frontend.py"

[tool.hatch.version]
path = "src/solace_agent_mesh/cli/__init__.py"
